# Brief A16 — Fix the rough edges the blind rehearsal exposed.

This repository, branch main. Do NOT commit; do NOT git checkout/switch/stash; pnpm only; you own packages/streamsmith/** and packages/mcpgen/** (runtime/generator only if needed). Keep every test suite green (streamsmith 97, mcpgen 79). Read docs/build/rehearsal-1.md fully and the A14 section of feedback/graph.md first.

1. **Offline sinkSchemaHash.** The receipt requires `sinkSchemaHash`, obtainable today only by dumping a live database (`schema-dump`). The from-proto DDL is deterministic from the spkg's proto descriptors + the sink's table rules (docs/build/substreams-facts.md (d); the real DDL the sink emitted is in runs/live/sink-a9-run1.err "table created" lines and runs/<runId>/schema.sql for the cloud). Add `streamsmith schema-render --spkg FILE [--out FILE]` that renders that DDL offline and prints its sha256, and make `receipt` accept `--schema-from-spkg` as an alternative to `--schema-sql/--schema-hash`. Prove equivalence with a test: rendering from packages/erc4626-flows/erc4626-flows-v0.1.0.spkg must produce, after normalizeSql, the same hash as the committed cloud dump runs/20260910T234439Z-1fr9/schema.sql (6d57b7cd…). If exact equivalence is impossible for a documented reason (e.g. server-side type aliasing like `timestamp`→`DateTime`), implement the same normalization on both sides and document it in README; do not weaken the check silently.
2. **`gate --reuse-runs` must not imply `--skip-build`.** Build unless `--skip-build` is passed; reuse only the run outputs. Test.
3. **SKILL.md (skills/streamsmith/SKILL.md) rough edges**, add a short "Known build-time traps" section from the rehearsal: (a) do not put a `sink:` block containing only `module:` in substreams.yaml (protogen rejects it); (b) if any ABI function takes uint256, add `num-bigint` as a direct dependency; `BigInt` has no From<&str>, use FromStr; (c) always pass `--limit-processed-blocks 0`; (d) each target database needs its own `--clickhouse-sink-info-folder`; (e) `deploy status` works without deploy.json. Keep SKILL.md under 150 lines.
4. mcpgen: compute `provenance.observedWindow.hours = (observedToTimestamp - observedFromTimestamp)/3600` (currently null although both timestamps are present; see runs/live/cloud/mcp-live-probe.txt). Regenerate packages/mcp-vaultflows. Test.
Report under 250 words: schema-hash equivalence result, test counts, files changed.
