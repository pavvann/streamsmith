# A22 — Seal the recorded one-prompt run into the repository

## Situation
The recorded one-prompt run happened in a sibling directory `streamsmith-recording` (outside this repository), built from a `clean-start` commit that held only `specs/`, the Streamsmith and mcpgen packages, and workspace files. The agent produced the package from the prompt alone: run id `20260913T114823Z-nyom`, gate passed (exit 0), published `erc4626-flows` **v0.1.1** to the registry (module hash `22c9d75e3161308ee9690d9fa1012eca10ac6ef3`, different from the reference package's `8e4892cf…` as expected for independently written code), deployed a self-managed sink into ClickHouse database `vaultflows_rec`, wrote receipt `receipts/erc4626-flows-v0.1.1-20260913T114823Z-nyom.json` with `mcpManifestHash`, and generated an MCP server. No video hash is being sealed; the evidence is the directory itself.

## Work
1. In the recording directory: `git add -A && git commit` with the message `One-prompt run: generated package and run evidence` (plus the usual trailer). Do not amend `clean-start`. Then export `git diff clean-start HEAD --stat` and the full patch `git format-patch clean-start..HEAD --stdout` for the evidence folder.
2. In this repository create `runs/recorded/20260913T114823Z-nyom/` containing: the run directory contents except `sinkinfo/`, `clickhouse-cursor.txt`, `sink.pid` and any file over 5 MB; the receipt copied verbatim (also copy it into `receipts/`); the generated package source (`packages/erc4626-flows` from the recording directory: `substreams.yaml`, `Cargo.toml`, `src/**`, `proto/**`, `sql/**` if present, NOT `target/` and NOT the `.spkg` — record the spkg sha256 and size in the README instead); the diff stat and patch from step 1; the `clean-start` and final commit hashes; and a `README.md` that states: the clean-start file count, what the prompt was (link to `specs/prompt.md`), start and end timestamps from the run files, gate result and assertion count, number of build attempts (count in `build.log`), publish result and registry URL, deploy database and rows at the time of the deploy record, the receipt hash, and the fact that the human typed one message and nothing else.
3. Scrub every copied text file for absolute machine paths (`/Users/...`), replacing the recording directory prefix with `<recording-dir>`; never change hashes, numbers, or block ranges. Logs are text and may be scrubbed the same way; if a scrub would alter a hashed artifact, do not copy that file and say so in the README.
4. Docs: `docs/SUBMISSION.md` (the "one-prompt → deployed pipeline recording" gate is now satisfied by `runs/recorded/…`; remove any wording that promises an uncut recording hash), `README.md` (a short "Recorded one-prompt run" entry in the evidence section pointing at the folder, with gate result, registry URL for v0.1.1, module hash, and time taken; remove any "uncut recording" claim), `docs/STATUS.md`, `docs/TASKS.md`. Project facts only; banned words: yield, APY, share price, TVL, risk.
5. `pnpm -r test` green (nothing under `packages/` changes, but confirm). Guardrail scan on all new files (`~/.claude/skills/git-guardrail/scan.sh`). Commit in small clean commits with the trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` (that trailer only; no session links), then `git push origin main` (normal push).

## Do not
Modify anything under `packages/`, `specs/` (other than this brief), `apps/`, or `runs/live/`. Do not start or stop processes. Do not touch the `vaultflows` or `vaultflows_hosted` databases. Do not put credentials or machine paths in tracked files or the report.

## Report
Evidence folder tree; recording-directory commit hash; files scrubbed; anything not copied and why; docs changed; commit subjects; test result; push confirmation with remote head.
