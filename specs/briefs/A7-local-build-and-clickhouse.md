# Brief A7 — Local build of erc4626-flows and local ClickHouse

Repo: /Users/pawan/Code/hacks/ethonline26, branch main. Do NOT commit, do NOT git checkout/switch/stash. Disk now has ~12 GB free; you may build. Two other agents are editing packages/streamsmith and packages/mcpgen concurrently: do not touch those directories or packages/erc4626-flows/sql/. You may only write under packages/erc4626-flows/ (build outputs, which are gitignored) , docs/build/, feedback/graph.md.

1. Docker: Docker Desktop was quit and its VM data deleted (it was corrupt). Start it (`open -a Docker`), wait until `docker info` succeeds (poll up to 3 min). Then follow docs/build/clickhouse-local.md exactly: run the ClickHouse container, create the `ro` user, verify `SELECT 1` over HTTP (8123) and native (9000). Fix the doc if any step is wrong. Accept: both checks pass.
2. Rust toolchain for the package: `packages/erc4626-flows/rust-toolchain.toml` pins a version (read it). `rustup toolchain install <that version> --profile minimal --target wasm32-unknown-unknown`. Verify `cargo --version` inside the package dir shows it.
3. Local build: in packages/erc4626-flows run `cargo test` (native), then `substreams build` and `substreams pack` (substreams CLI 1.22.0 is on PATH). Record wall-clock for each. Compare the local spkg's sha256 to the CI artifact: `gh run download 34396041869 -n <artifact name> -D /private/tmp/claude-501/-Users-pawan-Code-hacks-ethonline26/afb0fc58-eac0-4506-acb9-f92f2912ab7c/scratchpad/ci-spkg` (find the artifact name with `gh run view 34396041869`). Report whether hashes match; if not, explain the difference (spkg embeds paths/timestamps? check with `substreams info` on both).
4. `substreams info` on the local spkg: record module hashes into docs/build/toolchain.md under "Local build (Sept 10)".
5. Dry-run the gate's build step semantics: confirm `substreams build` exit code 0 and the output file path; note the exact path in docs/build/toolchain.md so Streamsmith's gate uses it.
6. If /Users/pawan/Code/hacks/ethonline26/.env exists and contains SUBSTREAMS_API_TOKEN, run the gate's primary range once: `substreams run -e base-mainnet.streamingfast.io:443 <local spkg> map_events --network base -s 51092254 -t 51092454 -o jsonl > /private/tmp/claude-501/-Users-pawan-Code-hacks-ethonline26/afb0fc58-eac0-4506-acb9-f92f2912ab7c/scratchpad/primary.jsonl` and report row counts per table and any errors. If no token, skip and say so.
7. Append friction to feedback/graph.md. Report under 300 words: ClickHouse status, toolchain, build timings, hash comparison, live-run result or "no token".
