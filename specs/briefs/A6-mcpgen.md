# Brief A6 — mcpgen (descriptor+receipt-driven MCP generator) and the vault-flows views

This repository, branch main. Do NOT commit, do NOT git checkout/switch/stash, do NOT run cargo/docker/substreams build (disk ~1.5 GB free). pnpm only; dependencies allowed: @modelcontextprotocol/sdk, zod, yaml (small). packages/streamsmith is being changed in parallel (A4b): do not touch that directory. You own packages/mcpgen, packages/mcp-vaultflows (generated output) and packages/erc4626-flows/sql/views.sql only.

Read first (20 min): docs/PROJECT.md §4.1 step 5 and §4.3 rule; specs/vaultflows.proto (tables come from `schema.table` options; injected columns per docs/build/substreams-facts.md (d), incl. `_deleted_`); docs/build/contract-notes.md; specs/receipt.schema.json; specs/streamsmith.yaml (views list, vaults, interval); docs/research/codex-discussion.md round 2 Q2 answer (what makes the MCP "not glue": typed semantic tools from the package's public types + receipt, read-only parameterized SQL, row limits/timeouts, freshness refusal, provenance in every response, schema-version fail-closed).

Deliverables:
1. packages/erc4626-flows/sql/views.sql — ClickHouse views over the from-proto tables (names exactly as the proto's schema.table options; column names = proto field names; always filter `_deleted_ = 0` if that column exists per facts (d), otherwise omit): `vault_flows_24h` (per vault: deposit_count, withdraw_count, assets_in_normalized, assets_out_normalized, net_assets_normalized over the trailing 24h from max(block_timestamp)), `share_value_growth` (per vault: first/last observation in the observed window with block numbers and timestamps, growth = last.assets_per_share_normalized / first.assets_per_share_normalized - 1, observed_hours; ONLY rows with call_ok = true). Banned words anywhere: yield, APY, share price, TVL, risk. Use "share-value growth" and "observed window".
2. packages/mcpgen — TypeScript CLI `generate --receipt <path> --proto <path to specs/vaultflows.proto> --views <views.sql> --out <dir>`: parses the proto (write a small parser for `message`/fields/`schema.table` options; no protoc dependency; document limits) and the receipt; emits a runnable MCP server package into --out with: one tool per table/view with typed zod params (vault filter restricted to the receipt's `parameters.vaults`, window hours, limit ≤ 500), parameterized read-only SQL over ClickHouse HTTP (env CLICKHOUSE_URL/USER/PASSWORD/DATABASE; `readonly=1` setting; 10 s timeout), a `pipeline_status` tool, and a `manifest.json` listing tools + hashes. Last stdout line must be exactly `{"manifest": "<absolute path>"}` (A4b's `streamsmith mcp` reads it).
3. Fail-closed contract in the generated server (real checks, not UI): on startup and every 60 s: (a) read the receipt; (b) query ClickHouse `system.tables`/`system.columns` for the expected tables and compare a normalized column-set hash to the receipt's `sinkSchemaHash` companion (store the expected column sets in manifest.json at generation time; if mismatch → every data tool returns an error object `{refused: true, reason: "schema_mismatch", expected, actual}`); (c) compute lag = eth_blockNumber (BASE_RPC_URL) − max(block_number) across tables; if lag > `MAX_LAG_BLOCKS` (default 300 ≈ 10 min) → refuse with `stale_data`; (d) every successful response carries `provenance: {packageHash, parametersHash, deploymentMode, deploymentId, headBlock, lagBlocks, observedWindow}`. Tools: `vault_flows`, `share_value_growth`, `recent_share_migration` (returns `{unavailable: true, reason: "share_transfers not populated in v0.1.0"}` if the table is empty), `pipeline_status`.
4. Run the generator against a FIXTURE receipt (write fixtures/receipt.example.json valid against specs/receipt.schema.json) to produce packages/mcp-vaultflows; vitest tests: proto parser on specs/vaultflows.proto, SQL builder never interpolates user strings (parameters only), refusal paths with a mocked ClickHouse client, manifest hash determinism. `pnpm typecheck` + `pnpm test` green in packages/mcpgen; generated package typechecks.
5. README for mcpgen (how it differs from a generic ClickHouse MCP: package types + receipt + fail-closed) and for the generated server (Claude Code / Claude Desktop config snippet). Append friction to feedback/graph.md.
Report under 400 words: tool list with param schemas, the exact fail-closed behaviors, what is mocked, open questions.
