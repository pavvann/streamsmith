# Brief A4b — Finish the Streamsmith CLI (continuation of A4)

This repository, branch main. A4 stopped mid-refactor; its work is committed as WIP. Do NOT commit, do NOT run git checkout/switch/stash, do NOT run cargo/rustup/docker/substreams build (disk ~1.5 GB free). pnpm only; add no dependency heavier than a few MB. Facts only; when a behavior depends on an external service you cannot reach (no Graph token, no ClickHouse), implement it against an injectable interface and cover it with a fixture test, then say so in the report.

State: `cd packages/streamsmith && pnpm typecheck` fails (tests reference exports the source renamed: buildDescriptor/contractDescriptor/packageDescriptor/normalizeFileDescriptorSet/descriptorFileNames in src/proto/descriptor.ts; parseJsonl/camelToSnake/callStatus in src/gate/jsonl.ts; DescriptorEvidence shape; GateRunSpec.endpoint). `pnpm test`: 22 pass / 14 fail / 8 skipped. Decide per case whether the source or the test is right, then make both agree. Source of truth for behavior is specs/gate.yaml.

Deliverables, in order:
1. Read specs/gate.yaml fully (19 assertions, warn-level flag, exit codes 0/10/20/30, two runs: primary 51092254:51092454 and observation 51092998:51093002, `-t` exclusive). Make `streamsmith gate` execute that file exactly. Every assertion listed there needs an evaluator: spec_unmodified, descriptor_hash_match (normalization algorithm is defined in gate.yaml; reproduce it byte-for-byte; the embedded Python reference there is the oracle: add a test that runs it via python3 and compares hashes), params_match, rows_gt, known_vault_present, reference_flows_present, log_index_matches_rpc (warn; public Base RPC https://mainnet.base.org, skip with a warning when offline), execution_rate_sanity, observation_present, observation_matches_reference (warn; eth_call convertToAssets at the cited block), rpc_success_ratio_gte, numeric_strings_valid, ids_unique, addresses_lowercase, deterministic_rerun, output_decodes_against_contract, banned_words, spkg_metadata. Warn-level never changes exit code. Write runs/<runId>/gate.json in the shape src/receipt.ts `gateEvidence` expects.
2. jsonl parsing must match the CURRENT proto (specs/vaultflows.proto): call_ok/call_error scalars (also tolerate the legacy nested call_status object), ids `{chain}-{block}-{logIndex}`, from_owner/to_owner, `"0"` numeric sentinel. Keep fixtures in sync.
3. `deploy`: after the sink has created base tables, apply `packages/erc4626-flows/sql/views.sql` if it exists (A6 is writing it in parallel; treat as optional file; run statements split on `;` via ClickHouse HTTP). Record `deploymentMode` honestly.
4. Add `streamsmith mcp` subcommand that shells out to `pnpm --filter @ethonline26/mcpgen generate --receipt <receiptPath> --out packages/mcp-vaultflows` and then records `mcpManifestHash` (sha256 of the generated manifest file the generator prints as its last stdout line, JSON `{"manifest": "<path>"}`) back into the receipt. Do not implement MCP generation yourself; A6 owns packages/mcpgen.
5. Plugin packaging matching vendor/substreams-skills layout: `.claude-plugin/plugin.json` (or marketplace.json if that is what the repo uses) at packages/streamsmith/, and `skills/streamsmith/SKILL.md` (<150 lines): use official substreams-* skills to write code; then `streamsmith gate`, `publish`, `deploy`, `receipt`, `mcp`, `manifest`, `casestudy`; never modify specs/*; on failure stop with evidence.
6. README.md: commands, env vars (.env.example: SUBSTREAMS_API_TOKEN, PORTAL_TOKEN, CLICKHOUSE_URL, CLICKHOUSE_USER, CLICKHOUSE_PASSWORD, CLICKHOUSE_DATABASE, BASE_RPC_URL), what the receipt binds, the fail-closed contract the MCP implements.
7. `pnpm typecheck` and `pnpm test` green in packages/streamsmith. Then run `pnpm -r typecheck` at repo root and make sure nothing else broke.
8. Append friction to feedback/graph.md. Report under 400 words: what is real vs stubbed, exact commands, open questions.
