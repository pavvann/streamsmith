# Brief A14 — Forensic one-prompt rehearsal in a fresh directory (the featured-challenge proof). Opus.

Goal: prove that, starting from ONLY the public contract + gate + tests + Streamsmith, a single human instruction makes Claude Code (with the official Substreams skills) produce a Substreams package that passes specs/gate.yaml, then publish, deploy, receipt, MCP. This rehearsal finds the rough edges; the recorded run happens later with a human watching. Facts only; log every rough edge to feedback/graph.md; do NOT fix the reference package to make things pass.

Hard rules: repo /Users/pawan/Code/hacks/ethonline26 on branch main is READ-ONLY for you except feedback/graph.md and docs/build/rehearsal-1.md. Work in a fresh directory /Users/pawan/Code/hacks/streamsmith-rehearsal-1 (create it; it is outside the repo). Do NOT commit anywhere; do NOT git checkout/switch/stash in the repo. You MAY run cargo/substreams build inside the rehearsal dir (disk ~9 GB; delete the rehearsal's target/ when done). You MAY use the network token in the repo .env for `substreams run` (gate ranges only: 51092254:51092454 twice and 51092998:51093002; always `--limit-processed-blocks 0`). You must NOT publish to the registry (version v0.1.0 already exists; a rehearsal publish would burn the namespace) — stop after the gate passes and do `streamsmith publish --dry-run`. You must NOT deploy to the cloud database (a live sink owns it) — use `deploy self-managed --stop-block` against the LOCAL Docker ClickHouse (clickhouse://sink:sinkpass@localhost:9000/vaultflows, container vaultflows-ch; start it with `docker start vaultflows-ch` if needed; drop the four tables first) or skip deploy and say so.

Steps:
1. Build the clean baseline: copy from the repo into the rehearsal dir ONLY: specs/ (vaultflows.proto, streamsmith.yaml, gate.yaml, prompt.md, receipt.schema.json), packages/streamsmith/ (without node_modules; run pnpm install there), packages/mcpgen/ (same), pnpm-workspace.yaml, package.json, .env (copy; gitignored). Do NOT copy packages/erc4626-flows or any Rust. Init a git repo in the rehearsal dir, commit, tag `clean-start`. Record `git status --porcelain`, `git rev-parse HEAD`, `git tag`, `date -u` into docs/build/rehearsal-1.md.
2. Read specs/prompt.md. That is the ONLY instruction you may act on for the build. Then, acting as the builder agent, use the official Substreams skills at /Users/pawan/Code/hacks/ethonline26/vendor/substreams-skills (read their SKILL.md files as the skills would be loaded) to author packages/erc4626-flows in the rehearsal dir from scratch: manifest importing the pinned Pinax erc4626 spkg URL from streamsmith.yaml, proto byte-identical to specs/vaultflows.proto, Rust modules satisfying the contract (first-sight probe, flows, fixed-interval observations, merged Events). You may NOT look at the reference implementation in the repo (packages/erc4626-flows) — that is the whole point. Iterate until `streamsmith gate` passes (build + the three runs + all assertions). Count iterations and note every failure and what fixed it.
3. Then `streamsmith publish --dry-run`, `streamsmith deploy self-managed … --stop-block 51093002` to LOCAL ClickHouse (or skip with reason), `streamsmith receipt`, `streamsmith mcp`, `streamsmith manifest start/finish`. Record timings.
4. Write docs/build/rehearsal-1.md: baseline evidence, the prompt used verbatim, iteration log (attempt → failure → fix), wall-clock per phase, final gate.json summary, receipt summary, the diff stats vs clean-start, and a candid list of everything that would make the RECORDED run smoother (skill gaps, missing docs in SKILL.md, flags the builder had to discover). Delete the rehearsal dir's target/ at the end. Report under 350 words with the iteration count and total wall-clock.
