# Brief A11 — Streamsmith ops gaps found in production use (small, bounded fixes)

This repository, branch main. Do NOT commit; do NOT git checkout/switch/stash; pnpm only; you own packages/streamsmith/** only (apps/vaultpilot is being changed in parallel). Keep `pnpm typecheck && pnpm test` green in packages/streamsmith. Facts and evidence for each item are in docs/build/sink-spike.md §6–§7 and runs/live/cloud/sink.err.

1. `deploy self-managed` must pass `--clickhouse-sink-info-folder <runs/<runId>/sinkinfo>` (and `--clickhouse-cursor-file-path` under the same run dir) to substreams-sink-sql. Reason: the sink stores a per-schema `vaultflows_schema_hash.txt` in that folder (default cwd); a file left by a previous run against a DIFFERENT database makes the sink skip CREATE TABLE and crash on insert. Also add `--final-blocks-only` by default with a `--no-final-blocks-only` opt-out. Add a test asserting the spawned argv.
2. `deploy views` resolves `--views` relative to `--root`, not to cwd, and must treat a missing file as an error when the flag is given explicitly (it is only "optional" when omitted). Apply statements one per HTTP request (ClickHouse HTTP rejects multi-statements). Test with a fixture views file.
3. `deploy status` must work with NO deploy.json when `--clickhouse-url` is given: compute head from `eth_blockNumber` (--rpc-url), max(_block_number_) from `_blocks_`, lag, row counts per table, and print/`--json`. Test with a mocked HTTP client.
4. `deploy hosted`: (a) omit `execution_config.parameters` unless `--params` is passed explicitly (the published spkg carries manifest defaults; the raw params string is rejected: `param for module "vaults[]": module not found`); (b) support `--update` to call `UpdateDeploymentConfig` on an existing `--deployment-id` instead of `Deploy`; (c) on `unauthenticated`, try `RefreshToken` with `PORTAL_REFRESH_TOKEN` once, then print the exact device-login instruction. Tests with fixture responses.
5. `receipt`: accept `--deploy-json` pointing at the output of `deploy status --json` for self-managed mode so `deploymentMode: self-managed-sink`, `headBlock`, `lagBlocks` are filled from live values. Populate `outputModuleHash`/`moduleHashes` from `substreams info --json` (the local spkg at packages/erc4626-flows/erc4626-flows-v0.1.0.spkg is real; read-only).
6. Update packages/streamsmith/README.md and SKILL.md for the above. Append friction to feedback/graph.md.
Report under 250 words: what changed, test counts, exact commands for: self-managed deploy to cloud, views, status, receipt.
