# Brief A10 — Vaultpilot decision service, Earn execution, one-screen UI. Opus.

Repo /Users/pawan/Code/hacks/ethonline26, branch main. Do NOT commit; do NOT git checkout/switch/stash; do NOT run cargo/docker/`substreams build`. pnpm only. You own apps/vaultpilot/** only. Other agents are editing packages/streamsmith and packages/erc4626-flows — never touch them; packages/mcpgen and packages/mcp-vaultflows are finished, read-only for you (import types/queries from them if useful).

Read first: docs/PROJECT.md §4.3 (the rule and the error paths, verbatim requirements), docs/build/privy-spike.md (real ids: wallet, policy, agent key quorum, vault ids), docs/build/privy-facts.md (Earn endpoints: position/deposit/withdraw + action polling), apps/vaultpilot/src/* (existing client, wallet, signer, policy, earn, spike), packages/mcp-vaultflows/manifest.json and README (tool names, params, refusal shape, provenance), packages/erc4626-flows/sql/views.sql (share_value_growth columns). Real credentials are in apps/vaultpilot/.env and the root .env (CH_CLOUD_* for ClickHouse Cloud, CLICKHOUSE_* for local Docker). The local ClickHouse has 2 observation rows and, after another agent finishes, 42 flow rows; ClickHouse Cloud is empty for now. Build against an injectable data source so tests use fixtures and the app works against either DB.

Deliverables:
1. `src/decision.ts` — pure function `decide(input) -> Decision` implementing PROJECT.md §4.3 exactly: inputs = current position vault, observations for both approved vaults (from `share_value_growth` view rows or a direct query: first/last observation block+timestamp+assets_per_share_normalized, call_ok only), 24h net outflow per vault, pipeline status (lag, refused?), cooldown state, last rotation block, config (minObservationHours=24, minDifferentialBps, cooldownHours, outflowGuardrailBps, maxLagBlocks=300). Output: `{action: 'hold'|'rotate', from, to, reason, evidence:{observedWindow, growthA, growthB, differentialBps, guardrail, lag}, refusals:[...]}`. Rules: refuse to act on stale/refused pipeline; require both vaults' observed window ≥ minObservationHours; rotate only if differential > minDifferentialBps; skip destination whose 24h outflow exceeds guardrail; exclude our own wallet's flows from the guardrail (owner/receiver == business wallet address or the Privy fee-wrapper address from `GET /v1/earn/ethereum/vaults/{id}`.vault_address); cooldown; idempotency key = `${fromVault}:${toVault}:${lastObservationBlock}`; never use the words yield/APY/share price/TVL/risk in code or UI copy ("share-value growth", "observed window").
2. `src/data.ts` — `VaultflowsSource` interface with two impls: `ClickHouseHttpSource` (read-only user, parameterized queries over HTTP with `readonly=1`, same SQL as the MCP's share_value_growth/vault_flows_24h tools; env selects cloud vs local) and `FixtureSource`. Also `pipelineStatus()` reading `_blocks_` max block vs `eth_blockNumber` (BASE_RPC_URL).
3. `src/executor.ts` — executes a rotation via the existing earn.ts as the AGENT signer: withdraw all from `from` (check `position.assets_in_vault`; respect per-action cap; use `raw_amount` if supported), poll the action to completion, then deposit into `to`; poll; if the deposit fails after a successful withdraw → leave USDC idle, write an alert record, do not retry automatically. Persist a JSON ledger `apps/vaultpilot/.vaultpilot-ledger.json` (gitignored) with actions, action ids, tx hashes, decision evidence. Enforce the app-side daily cap (VAULTPILOT_DAILY_CAP_USD) from the ledger.
4. `src/loop.ts` — `pnpm agent:once` runs one cycle (read data → decide → execute if rotate and not dry-run → print a one-screen text summary); `pnpm agent:watch` every N minutes. Default DRY_RUN=true; real execution only with `--execute`.
5. `src/demo.ts` — `pnpm demo:denied` attempts an earn_deposit to a non-allowlisted vault id and prints the denial; `pnpm demo:deposit --amount 25` deposits into the vault with the LOWER observed growth (so a real rotation is possible later) and prints tx hashes + position; `pnpm demo:rotate` runs one cycle with `--execute`. Each prints Basescan links (https://basescan.org/tx/<hash>). Do not run anything that moves money unless the wallet balance > 0 and the human has asked; the orchestrator will run these.
6. UI: a minimal Next.js 15 app (App Router, no UI library, pnpm) in apps/vaultpilot/web (or a `web/` folder within the package if simpler) with ONE page reading the ledger + live decision via a route handler: current position and vault name; the two vaults' observed windows (hours, block range) and share-value growth; the decision and the rule that produced it; the policy summary (allowed vault ids, cap) and the latest allow/deny outcomes; pipeline provenance (packageHash, outputModuleHash, headBlock, lag, deploymentMode) from the MCP's `pipeline_status` shape; tx links; who controls the wallet (treasurer key id) and the revocation command. Keep the install small (disk has ~9 GB free). `pnpm --filter <web> build` must pass.
7. Tests (vitest): decision rules (hold on stale, hold under min window, rotate on differential, guardrail blocks destination, self-flows excluded, cooldown, idempotency), executor partial-failure path with a mocked earn client, daily cap.
`pnpm typecheck` and `pnpm test` green in apps/vaultpilot; root `pnpm -r typecheck` green. Append friction to feedback/privy.md. Report under 350 words: commands, what is real vs mocked, what needs funding to verify, open questions.
