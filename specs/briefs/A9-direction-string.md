# Brief A9 — Replace the FlowDirection enum with a string; rebuild; land the 42 flow rows in ClickHouse.

This repository, branch main. Do NOT commit; do NOT git checkout/switch/stash. You own: specs/vaultflows.proto, specs/gate.yaml, docs/build/contract-notes.md, packages/erc4626-flows/** (except sql/), runs/live/**, docs/build/sink-spike.md, feedback/graph.md. Do NOT touch packages/streamsmith, packages/mcpgen, packages/mcp-vaultflows, packages/erc4626-flows/sql (they belong to parallel workstreams and will be updated for this change). Facts only.

Why: `substreams-sink-sql` 4.13.1 (latest release) from-proto mode panics on proto3 enum fields (`interface {} is protoreflect.EnumNumber, not int32`, accumulator_inserter.go), so `VaultFlow.direction` (enum FlowDirection) blocks every flow row. See docs/build/sink-spike.md.

Do, in order:
1. specs/vaultflows.proto: delete `enum FlowDirection`; change field 12 to `string direction = 12;` with comment: values exactly `deposit` or `withdraw`. Fix the header comment about enums (line ~24). Keep every other field number. Run `buf lint` (buf 1.72.0 on PATH; same lint exceptions A2b used). Recompute the normalized descriptor hash using the Python reference embedded in specs/gate.yaml (`spec_unmodified`) and update that expected hash value in gate.yaml; document the algorithm run and the new hash in docs/build/contract-notes.md under "Proto changes by A9".
2. specs/gate.yaml: reference rows `direction: FLOW_DIRECTION_DEPOSIT/WITHDRAW` → `direction: deposit` / `direction: withdraw` (quoted strings). Any other enum mention likewise.
3. Rust (packages/erc4626-flows): copy the proto into the package (must stay byte-identical to specs/vaultflows.proto; CI enforces), regenerate pb bindings the same way A5 did (see build.rs / buf.gen.yaml / Makefile or the commands recorded in docs/build/toolchain.md), replace `FlowDirection::Deposit/Withdraw` and `direction as i32` with the strings, update unit tests, `cargo test` (native), `substreams build`, `substreams pack` (rust 1.88 pinned via rust-toolchain.toml; ~35 s). Update packages/erc4626-flows/README.md.
4. Regenerate live evidence with the new spkg (token: `set -a; source .env; set +a`): `substreams run -e base-mainnet.streamingfast.io:443 packages/erc4626-flows/erc4626-flows-v0.1.0.spkg map_events --network base -s 51092254 -t 51092454 -o jsonl --limit-processed-blocks 0 > runs/live/primary-51092254-51092454.jsonl` and the observation range 51092998:51093002 into runs/live/observation-51092998-51093002.jsonl. Verify 42 flows (33 dep + 7 wd Gauntlet, 1 + 1 Steakhouse) with `direction` strings. Update runs/live/README.md.
5. Sink: drop and recreate the local tables (`docker exec vaultflows-ch clickhouse-client --user sink --password sinkpass --multiquery` — see docs/build/clickhouse-local.md), then run exactly A8's primary command from docs/build/sink-spike.md §1 (range 51092254:51093002, cursor file runs/live/clickhouse-cursor.txt, delete the old cursor file first). Verify `SELECT count() FROM vaultflows.vault_flows` = 42, `share_value_observations` = 2, sample rows, restart-safety (rerun, counts unchanged). Do NOT apply views.sql (it references `direction = 1/2` and is being updated in parallel). Update docs/build/sink-spike.md with a "§6 After enum→string (A9)" section with commands, timings, counts.
6. Do not push the branch and do not run `gh`; CI runs on the project commit.
Report under 300 words: hashes (new spec descriptor hash, map_events module hash), row counts, timings, anything unverified.
