# Brief A15 — Root README, architecture diagram, sponsor evidence map. Sonnet.

Repo /Users/pawan/Code/hacks/ethonline26, branch main. Do NOT commit; do NOT git checkout/switch/stash; you own README.md (root, new), docs/ARCHITECTURE.md (new), docs/diagram.mmd (new) and nothing else. Read first: docs/PROJECT.md §1 (thesis paragraph, use verbatim), §4, §6 (sponsor table), §7; docs/STATUS.md (what is actually live); receipts/*.json (the real receipt); runs/live/cloud/mcp-live-probe.txt (real MCP output); docs/build/vaults.md, sink-spike.md §6–§7, privy-spike.md; packages/*/README.md and apps/vaultpilot/README.md. Banned words anywhere: yield, APY, share price, TVL, risk.

Write:
1. README.md (root): the thesis paragraph from PROJECT.md §1 verbatim at the top; one-paragraph "what is live right now" with concrete numbers pulled from the receipt and probe (module hash, package on substreams.dev, ClickHouse Cloud, lag, the share_value_growth numbers with their observed window); the architecture (Mermaid, see 3); a "Run it" section per package with the exact commands that exist in each package README; a **Sponsor evidence** section with two subsections, The Graph (Composable/Standardized; AI Tooling incl. the featured one-prompt challenge) and Privy (B2B; Financial flow), each listing the qualification bullets from docs/PRIZES.md and, for each bullet, the exact file/line or artifact that satisfies it (link to receipts/…json, runs/live/…, specs/gate.yaml, apps/vaultpilot/src/policy.ts, docs/build/privy-spike.md); an honest **Limitations** section (self-managed sink vs hosted status, 10% wrapper fee, observed-window semantics, no rotation executed yet if still true, packageHash not reproducible); links to AI-USAGE.md, feedback/, docs/research/codex-discussion.md. Do not claim anything not evidenced in the repo.
2. docs/ARCHITECTURE.md: one page, the promotion pipeline (prompt → skills → gate → publish → deploy → receipt → MCP → Vaultpilot → Privy → Base), what each hash in the receipt binds, and the fail-closed contract with the exact refusal reasons from packages/mcp-vaultflows/README.md.
3. docs/diagram.mmd: a Mermaid flowchart matching (2), also embedded in README. Keep it readable at 900 px.
Report under 200 words: what you wrote, any claim you could NOT evidence and left out.
