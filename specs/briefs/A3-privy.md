# Brief A3 — Privy facts and Vaultpilot server skeleton

This repository. Do not commit. pnpm only. No credentials exist yet: build so that everything runs the moment .env is filled; do not fake results. Tools: firecrawl (cap 15 scrapes, save under <scratchpad>/.firecrawl/build/), WebFetch/WebSearch, curl. Try https://docs.privy.io/llms.txt and llms-full.txt first (cheap, complete). Every fact cited with URL. Log friction to feedback/privy.md.

1. docs/build/privy-facts.md with exact, cited answers: (a) Node SDK package name + init (app id, app secret, authorization key); (b) create a server wallet (EVM) and read its address; (c) authorization keys / "additional signers": how to attach a second signer to a wallet and how to remove it (revocation); (d) policies: schema for a rule with method `earn` (or whatever method governs Earn deposit/withdraw), condition fields available to restrict to specific vault ids and cap amount; also `eth_sendTransaction` caps and rolling aggregations; (e) Earn API: endpoints for list vaults / position / deposit / withdraw, request bodies, chain, the vault identifiers for Gauntlet USDC Prime and Steakhouse Prime, gas sponsorship requirement; (f) Organizations and key quorums: creation endpoints and any "contact sales" gating language; (g) webhooks list (for later); (h) rate limits, sandbox/test mode existence. Quote the docs.
2. Scaffold apps/vaultpilot as a pnpm workspace package (TypeScript, Node 20+): `src/privy.ts` (client init from env), `src/wallet.ts` (createBusinessWallet, getWallet), `src/signer.ts` (createAgentAuthorizationKey, attachAgentSigner(walletId, keyId, policyId), detachAgentSigner), `src/policy.ts` (buildEarnPolicy({allowedVaultIds, perActionCapUsd, dailyCapUsd}) returning the exact JSON per docs; createPolicy), `src/earn.ts` (position, deposit, withdraw for a vaultId), `src/spike.ts` (sequence: create wallet → policy → attach signer → read both positions → attempt deposit to a NON-allowed vault id expecting denial → print results; exits with a clear message listing missing env vars if any are unset). `.env.example` with PRIVY_APP_ID, PRIVY_APP_SECRET, PRIVY_AUTH_KEY, PRIVY_AGENT_AUTH_KEY, PRIVY_VAULT_ID_GAUNTLET, PRIVY_VAULT_ID_STEAKHOUSE, BASE_RPC_URL. `package.json` scripts: `spike`, `typecheck`. Install deps with pnpm; run `pnpm typecheck` and make it pass.
3. Report (under 400 words): the policy JSON you drafted, the Earn endpoints, gating findings (Organizations/quorums), anything the docs contradict in docs/research/arc-privy-ledger.md §1.3, and what the human must do in the dashboard (step list).
