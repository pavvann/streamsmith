# Brief A2 — Substreams facts, vault resolution, public contract

This repository. Do not commit. Tools: curl for raw GitHub (free), `firecrawl scrape "<url>" -o <file>` (cap 15 scrapes; save under <scratchpad>/.firecrawl/build/), WebFetch/WebSearch. No browser automation. Every fact needs a source (file path + quote, or URL). Mark anything unverified.

1. `git clone --depth 1 https://github.com/streamingfast/substreams-skills vendor/substreams-skills` (path is gitignored via vendor/? if not, add `vendor/` to .gitignore). Read SKILL.md (and referenced docs) for: substreams-dev, substreams-ethereum, substreams-sql, substreams-hosted-sink, thegraph-market-api, substreams-testing. Extract into docs/build/substreams-facts.md: (a) manifest `params:` mechanism and how a module reads params; (b) eth_call / RpcBatch: at which block does a call execute during historical replay? quote the doc; (c) store module patterns for first-sight caching; (d) from-proto SQL sink rules for ClickHouse (single PK, ORDER BY, cursor table, insert-only, schema evolution); (e) hosted sink: exact Portal API calls/sequence for deploy, attach DB, secrets, GetDeploymentState; device-code auth steps; (f) `substreams registry publish` / pack commands; (g) testing skill: how tests/fixtures are structured; (h) the case-study format used in the repo (find the 16 case studies; note path + structure).
2. Resolve the two pinned vaults into docs/build/vaults.md: Privy self-serve Earn vaults named "Gauntlet USDC Prime" and "Steakhouse Prime" (Morpho, Base). Find: on-chain vault address on Base (Morpho app, Basescan, or Morpho docs/API), Privy vault id used by Privy's Earn API (docs.privy.io — try https://docs.privy.io/llms.txt or llms-full.txt first, cheap), share decimals, asset address (expect Base USDC 0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913, 6 decimals), and one recent Base block range (≤200 blocks) containing a Deposit or Withdraw on one of them (Basescan logs). Cite every URL.
3. Read Pinax erc4626 proto: https://raw.githubusercontent.com/pinax-network/substreams-evm/main/proto/v1/erc4626.proto (find correct path in repo). Design and write: specs/vaultflows.proto (package `vaultflows.v1`; messages VaultFlow {chain_id, block_number, block_hash, block_timestamp, tx_hash, log_index, vault, caller, owner, receiver, direction, assets_raw, shares_raw, assets_normalized, shares_normalized, asset_decimals, share_decimals, execution_rate (deposit- or withdraw-implied), meta_valid, call_status}, ShareValueObservation {chain_id, block_number, block_hash, block_timestamp, vault, sample_interval_blocks, assets_per_share_raw, assets_per_share_normalized, total_assets_raw, total_supply_raw, call_status}, ShareTransfer (optional), CallStatus {ok, error} and the wrapper Events messages). Banned words in names/comments: yield, APY, share price, TVL, risk. Document each field in docs/build/contract-notes.md.
4. Write specs/streamsmith.yaml (chainId 8453, network base, packageName erc4626-flows, version 0.1.0, imports: pinax erc4626 (+erc20 optional), vaults: [the two addresses], sampleIntervalBlocks: 1800, sink: clickhouse from-proto, startBlock: ~6 weeks before head as a number you compute from Base 2s blocks and the current head you look up) and specs/gate.yaml (build; run range = the range from step 2 padded to 200 blocks; assertions: rows_gt 0, known_vault_present, rpc_success_ratio_gte 0.99, deterministic_rerun, descriptor_hash_match against specs/vaultflows.proto).
5. Copy the verbatim one-human-instruction from docs/PROJECT.md §5 into specs/prompt.md.
6. Report (under 500 words): the facts that change the design (esp. eth_call block context and params), the two vault addresses + Privy ids + block range with sources, and what you left unverified.
