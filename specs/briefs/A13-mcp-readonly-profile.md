# Brief A13 — mcpgen runtime: work with a ClickHouse read-only PROFILE user; verify against the live cloud. Opus.

Repo /Users/pawan/Code/hacks/ethonline26, branch main. Do NOT commit; do NOT git checkout/switch/stash; pnpm only; you own packages/mcpgen/** and packages/mcp-vaultflows/** only (another agent owns packages/streamsmith). Only read-only ClickHouse SELECTs and eth_blockNumber against real services; a live sink is writing to the cloud DB — never write to it.

Verified failure (orchestrator, 05:39 UTC, generated server against ClickHouse Cloud as user `ro`, creds in repo .env as CH_CLOUD_URL / CH_CLOUD_RO_USER / CH_CLOUD_RO_PASSWORD / CH_CLOUD_DATABASE): every guardian query fails `HTTP 500 Code: 164 Cannot modify 'max_execution_time' setting in readonly mode`, so pipeline_status reports `check_unavailable` and every data tool refuses. Root cause: the runtime sends `readonly=1` and `max_execution_time` as request settings; ClickHouse Cloud's `ro` user has a readonly PROFILE (readonly=1 at profile level), and such users may not set ANY settings, including readonly itself. Vaultpilot's data source already handles this (apps/vaultpilot/src/data.ts: retry once without settings and record the mode) — mirror that behavior.

Do:
1. In packages/mcpgen/runtime (the ClickHouse client used by the generated server): on HTTP 500 with body containing `Code: 164` and `readonly mode`, retry the identical query once with NO settings parameters, cache a flag `settingsMode: "none"` for the process lifetime, and surface it in `pipeline_status` and `provenance` (e.g. `clickhouse: {settingsMode: "readonly-profile"}`). Keep the timeout enforced client-side (AbortController) so removing `max_execution_time` loses nothing. Add unit tests with a mocked fetch for: first 500/164 → retry without settings → success; unrelated 500 → no retry; the cached mode persisting.
2. Regenerate packages/mcp-vaultflows from the CURRENT receipt fixture with the generator (pnpm --filter @ethonline26/mcpgen generate … exactly as documented) so the runtime fix lands in the generated server; `pnpm typecheck` in both packages green; `pnpm test` in mcpgen green.
3. Real verification (read-only): from repo root `set -a; source .env; set +a; export CLICKHOUSE_URL="$CH_CLOUD_URL" CLICKHOUSE_USER=ro CLICKHOUSE_PASSWORD="$CH_CLOUD_RO_PASSWORD" CLICKHOUSE_DATABASE=vaultflows BASE_RPC_URL=https://mainnet.base.org; node packages/mcp-vaultflows/bin/server.js` and drive it over stdio (initialize → tools/list → tools/call pipeline_status → tools/call share_value_growth {limit:5}). Expected now: pipeline_status shows a real headBlock and lag; if lag > 300 the verdict is `stale_data` (correct while the backfill is behind, that is the fail-closed behavior working) and data tools refuse with `stale_data`; if lag ≤ 300 they answer with provenance. Paste the JSON in your report. NOTE the fixture receipt's `sinkSchemaHash`/`outputModuleHash`/`deploymentId` are fixture values, so `receipt_mismatch` or `schema_mismatch` may legitimately fire against the real DB; report exactly which check fires and why, do not loosen the checks. The real receipt will be generated by the orchestrator after `deploy status` is fixed.
Append friction to feedback/graph.md. Report under 250 words.
